import 'dart:ui';

import 'package:flutter/material.dart';

const Color colorA = Color(0xFF5465FF); // أزرق مائل للموف
const Color colorB = Color(0xFF788BFF); // أزرق فاتح
const Color colorC = Color(0xFF9BB1FF); // أزرق شاحب فاتح
const Color colorD = Color(0xFFBFD7FF); // أزرق فاتح جدًا
const Color colorE = Color(0xFFE2FDFF); // أزرق شاحب
const Color colorV = Color(0xFFE7F0FD);

// Primary brand color
const Color primary = Color(0xFF4A90E2); // الأزرق الهادي

// Secondary support color
const Color secondary = Color(0xFF50E3C2); // التركواز الفاتح

// Backgrounds
const Color background = Color(0xFFF7F9FC); // خلفية التطبيق

// Card and surfaces
const Color card = Color(0xFFFFFFFF); // للكروت والعناصر البيضاء

// Text colors
const Color textPrimary = Color(0xFF2C3E50); // للنصوص الأساسية
const Color textSecondary = Color(0xFF7F8C8D); // للنصوص الثانوية

// Accent color (buttons/icons)
const Color accent = Color(0xFFF5A623); // لون تفاعلي ناعم

// Optional: Shadow
const Color shadow = Color(0x1A000000); // ظل خفيف

class AppColors {  static const Color searchBar = Colors.white;

static const Color primary = Color(0xFF4C5CFF); // أزرق بنفسجي هادئ
static const Color secondary = Color(0xFF6C63FF); // بنفسجي فاتح
static const Color background = Color(0xFFF7F8FC); // رمادي فاتح مريح
static const Color card = Colors.white;
static const Color shadow = Color(0x1A000000); // ظل خفيف
static const Color textPrimary = Color(0xFF1E1E1E);
static const Color textSecondary = Color(0xFF6E6E6E);
static const Color border = Color(0xFFE0E0E0);
static const Color icon = Color(0xFF616161);
static const Color accent = Color(0xFF00C897); // لون زر أكشن (أخضر نعناعي)
static const Color rating = Color(0xFFFFC107); // أصفر للتقييم
}
